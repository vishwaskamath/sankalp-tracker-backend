spring.application.name=sankalp-tracker

# GraphQL Configuration
graphql.servlet.mapping=/graphql
graphql.playground.enabled=true
graphql.playground.mapping=/playground

# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/sankalp_tracker?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Kamath@123&456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
# Creates/updates tables based on entities (comment moved to a new line)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect